class Solution(object):
    def largestAltitude(self, gain):
        """
        :type gain: List[int]
        :rtype: int
        """
        altitude = 0
        ans = 0
        for g in gain:
            altitude += g
            ans = max(ans, altitude)
        return ans
